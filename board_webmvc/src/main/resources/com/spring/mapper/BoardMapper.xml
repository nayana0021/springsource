<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.BoardMapper">

	<!-- 전체조회 -->
	<select id="list" resultType="com.spring.domain.BoardDTO">
		select bno,title,writer,regdate,updatedate from spring_board order by bno desc, regdate desc
	</select>
	
	<!-- 글 등록 --> <!-- 날짜는 자동으로 디폴트가 sysdate 다 -->
	<insert id="insert">
		insert into spring_board (bno,title,content,writer)
		values(seq_board.nextval,#{title},#{content},#{writer})
	</insert>
	
	<!-- 특정 번호 조회 : bno 일치하는 내용 조회 -->
	<select id="read">
		select title,content,writer form spring_board where bno=#{bno}
	</select>
</mapper>